class AppConfig {
  // Must match your backend
  static const String baseUrl = "https://legacy.globalprints.pk/metal";

  // How often the app refreshes UI data
  static const int refreshSeconds = 120;

  // Default currency on first open
  static const String defaultCurrency = "PKR";
}
